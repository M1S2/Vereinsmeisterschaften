<dialogs:CustomDialog x:Class="Vereinsmeisterschaften.Controls.DoubleProgressDialog"
                      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
                      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
                      xmlns:local="clr-namespace:Vereinsmeisterschaften.Controls"
                      xmlns:properties="clr-namespace:Vereinsmeisterschaften.Properties"
                      xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
                      xmlns:dialogs="clr-namespace:MahApps.Metro.Controls.Dialogs;assembly=MahApps.Metro"
                      xmlns:helpers="clr-namespace:Vereinsmeisterschaften.Helpers"
                      mc:Ignorable="d" 
                      d:DesignHeight="450" d:DesignWidth="800"
                      xmlns:Dialog="clr-namespace:MahApps.Metro.Controls.Dialogs;assembly=MahApps.Metro"
                      x:Name="root"
                      Title="{Binding Title, ElementName=root}">

    <!-- Style taken from https://github.com/punker76/MahApps.Metro.CustomDialog.Sample/blob/master/TinyLittleMvvm/MahApps.CustomDialog.Sample/ChoiceDialogView.xaml -->
    <dialogs:CustomDialog.Style>
        <Style BasedOn="{StaticResource {x:Type dialogs:BaseMetroDialog}}" TargetType="{x:Type dialogs:CustomDialog}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type dialogs:CustomDialog}">
                        <Grid Background="{TemplateBinding Background}">
                            <Border FocusVisualStyle="{x:Null}" Focusable="False">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="*"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <ContentPresenter Grid.Row="0" Content="{TemplateBinding DialogTop}"/>
                                    <Grid Grid.Row="1">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="5*"/>
                                            <ColumnDefinition Width="80*"/>
                                            <ColumnDefinition Width="5*"/>
                                        </Grid.ColumnDefinitions>
                                        <!--  Content area  -->
                                        <Grid Grid.Column="1" Margin="0,10,0,0">
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="*"/>
                                            </Grid.RowDefinitions>
                                            <TextBlock Grid.Row="0" FontSize="{DynamicResource MahApps.Font.Size.Dialog.Title}" Foreground="{TemplateBinding Foreground}" Text="{TemplateBinding Title}" TextWrapping="Wrap"/>
                                            <ContentPresenter Grid.Row="1" Content="{TemplateBinding Content}" />
                                        </Grid>
                                    </Grid>
                                    <ContentPresenter Grid.Row="2" Content="{TemplateBinding DialogBottom}"/>
                                </Grid>
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <EventTrigger RoutedEvent="Loaded">
                                <EventTrigger.Actions>
                                    <BeginStoryboard Storyboard="{StaticResource MahApps.Storyboard.Dialogs.Show}"/>
                                </EventTrigger.Actions>
                            </EventTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </dialogs:CustomDialog.Style>

    <dialogs:CustomDialog.DialogBottom>
        <!-- Cancel Button -->
        <Button DockPanel.Dock="Bottom" Margin="20" HorizontalAlignment="Right" VerticalAlignment="Center"
                Command="{Binding CancelCommand, ElementName=root}"
                Content="{x:Static properties:Resources.CancelString}" FontSize="{DynamicResource MahApps.Font.Size.Dialog.Button}"
                Style="{StaticResource MahApps.Styles.Button.Dialogs.Accent}"/>
    </dialogs:CustomDialog.DialogBottom>

    <dialogs:CustomDialog.Content>
        <DockPanel LastChildFill="True">
            <TextBlock DockPanel.Dock="Top" Margin="0,15" Text="{Binding Message, ElementName=root}" FontSize="{DynamicResource MahApps.Font.Size.Dialog.Message}" TextWrapping="Wrap" />

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding ProgressDescription1, ElementName=root}" VerticalAlignment="Center"/>
                <TextBlock Grid.Row="1" Grid.Column="0" Margin="0,0,5,0" VerticalAlignment="Center" TextAlignment="Right"
                           helpers:StringFormatHelper.Format="{Binding ProgressFormatString, ElementName=root}"
                           helpers:StringFormatHelper.Value="{Binding Progress1, ElementName=root}"
                           Text="{Binding (helpers:StringFormatHelper.FormattedValue), RelativeSource={RelativeSource Mode=Self}}"/>
                <controls:MetroProgressBar Grid.Row="1" Grid.Column="1" Margin="0,5" Height="15" Value="{Binding Progress1, ElementName=root}"/>

                <TextBlock Grid.Row="2" Grid.Column="1" Margin="0,20,0,0" Text="{Binding ProgressDescription2, ElementName=root}" VerticalAlignment="Center"/>
                <TextBlock Grid.Row="3" Grid.Column="0" Margin="0,0,5,0" VerticalAlignment="Center" TextAlignment="Right"
                           helpers:StringFormatHelper.Format="{Binding ProgressFormatString, ElementName=root}"
                           helpers:StringFormatHelper.Value="{Binding Progress2, ElementName=root}"
                           Text="{Binding (helpers:StringFormatHelper.FormattedValue), RelativeSource={RelativeSource Mode=Self}}"/>
                <controls:MetroProgressBar Grid.Row="3" Grid.Column="1" Margin="0,5" Height="15" Value="{Binding Progress2, ElementName=root}"/>
            </Grid>
        </DockPanel>
    </dialogs:CustomDialog.Content>
</dialogs:CustomDialog>
