<UserControl x:Class="Vereinsmeisterschaften.Controls.MultiSelectComboBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="clr-namespace:Vereinsmeisterschaften.Converters"
             x:Name="root">
    <UserControl.Resources>
        <converters:SelectedItemsContainsConverter x:Key="SelectedItemsContainsConverter" />
    </UserControl.Resources>
    
    <Grid>
        <Border BorderThickness="1" BorderBrush="{DynamicResource MahApps.Brushes.ComboBox.Border.Focus}" Padding="4" CornerRadius="3"
                Background="{DynamicResource MahApps.Brushes.ComboBox.Background}"
                MinHeight="24"
                VerticalAlignment="Center"
                PreviewMouseLeftButtonDown="OnTogglePopup">
            <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
                <ItemsControl ItemsSource="{Binding SelectedItems, ElementName=root}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="{DynamicResource MahApps.Brushes.Gray2}" CornerRadius="10" Margin="2" Padding="4,0">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding}" Foreground="{DynamicResource MahApps.Brushes.Text}" VerticalAlignment="Center"/>
                                    <Button Content="✕" Style="{DynamicResource MahApps.Styles.Button.Circle}" Background="{StaticResource BrushError}"
                                            Width="20" Height="20"
                                            Margin="4,0,0,0"
                                            Command="{Binding ElementName=root, Path=RemoveItemCommand}"
                                            CommandParameter="{Binding}" />
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>

        <!-- Popup Dropdown -->
        <Popup x:Name="PART_Popup"
               PlacementTarget="{Binding ElementName=root}"
               Placement="Bottom"
               StaysOpen="True"
               Width="{Binding ActualWidth, ElementName=root}">
            <Border Background="{DynamicResource MahApps.Brushes.ComboBox.PopupBackground}" 
                    BorderBrush="{DynamicResource MahApps.Brushes.ComboBox.Border}" 
                    BorderThickness="1" CornerRadius="3" Padding="4">
                <ScrollViewer MaxHeight="200">
                    <ItemsControl ItemsSource="{Binding ItemsSource, ElementName=root}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <CheckBox Checked="OnChecked" Unchecked="OnUnchecked"
                                          Foreground="{DynamicResource MahApps.Brushes.ComboBox.PopupForeground}">
                                    <CheckBox.IsChecked>
                                        <MultiBinding Mode="OneWay" Converter="{StaticResource SelectedItemsContainsConverter}">
                                            <Binding ElementName="root" Path="SelectedItems"/>
                                            <Binding Path="." />
                                        </MultiBinding>
                                    </CheckBox.IsChecked>
                                    <CheckBox.Content>
                                        <ContentPresenter Content="{Binding}" ContentTemplate="{Binding ElementName=root, Path=ItemTemplate}" />
                                    </CheckBox.Content>
                                </CheckBox>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Border>
        </Popup>
    </Grid>
</UserControl>
